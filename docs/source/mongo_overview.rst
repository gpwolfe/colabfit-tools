.. note::

   This documentation described the legacy MongoDB implementation, which has
   been deprecated. ColabFit now uses PostgreSQL (see :mod:`colabfit.tools.pg`)
   and VastDB (see :mod:`colabfit.tools.vast`) backends.
